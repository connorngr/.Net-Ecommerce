@page
@model LoginModel

@{
    ViewData["Title"] = "Sign in";
    Layout = "~/Areas/Identity/Pages/_AuthLayout.cshtml";
}

@* <div class="container" id="container">
    <div class="form-container sign-in">
        <form id="external-account" asp-page="ExternalLogin" asp-route-returnUrl="@Model.ReturnUrl" method="post">
            <h1>Sign In</h1>
            <div class="social-icons">
                @foreach (var provider in Model.ExternalLogins!)
                {
                    <button type="submit" class="icon" name="provider" value="@provider.Name" title="Log in using your @provider.DisplayName account"><i class="fa-brands fa-google-plus-g"></i></button>
                    <button type="submit" class="icon" name="provider" value="@provider.Name" title="Log in using your @provider.DisplayName account"><i class="fa-brands fa-facebook-f"></i></button>
                }
            </div>
        </form>
        <form id="account" method="post">
            <span>or use your email password</span>
            <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>

            <input asp-for="Input.Email" aria-required="true" placeholder="email@gmail.com">
            <span asp-validation-for="Input.Email" class="text-danger"></span>

            <input asp-for="Input.Password" aria-required="true" placeholder="Password">
            <span asp-validation-for="Input.Password" class="text-danger"></span>

            <a id="forgot-password" asp-page="ForgotPassword">Forgot your password?</a>
            <button id="login-submit" type="submit" class="button">Sign In</button>
        </form>
    </div>
    <div class="toggle-container">
        <div class="toggle">
            <div class="toggle-panel toggle-right">
                <h1>Hello, Friend!</h1>
                <p>Register with your personal details to use all of site features</p>
                <a asp-page="Register" class="hidden button" id="register">Sign Up</a>
            </div>
        </div>
    </div>
</div> *@

<div class="container" id="container">
    <div class="login-section">
        <div class="form-box Register">
            <form id="loginForm" asp-route-returnUrl="@Model.ReturnUrl" method="post">
                <h2>Login</h2>
                <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>

                <div class="input-box">
                    <input asp-for="Input.Email" aria-required="true">
                    <label asp-for="Input.Email">Email</label>
                    <span asp-validation-for="Input.Email" class="text-danger"></span>
                </div>

                <div class="input-box">
                    <input asp-for="Input.Password" aria-required="true">
                    <label asp-for="Input.Password">Password</label>
                    <span asp-validation-for="Input.Password" class="text-danger"></span>
                </div>

                <a id="forgot-password" asp-page="ForgotPassword">Forgot your password?</a>

                <div class="input-box">
                    <button id="registerSubmit" type="submit" class="btn">Login</button>
                </div>
                <div>
                    <button id="login-submit" type="submit" class="w-100 btn btn-lg btn-primary">Log in</button>
                </div>
                <div class="mt-3">
                    <p>
                        <a id="forgot-password" class="text-decoration-none" asp-page="./ForgotPassword">Forgot your password?</a>
                    </p>
                    @* <p>
                        <a asp-page="./Register" class="text-decoration-none" asp-route-returnUrl="@Model.ReturnUrl">Register as a new user</a>
                    </p>
                    <p>
                        <a id="resend-confirmation" class="text-decoration-none" asp-page="./ResendEmailConfirmation">Resend email confirmation</a>
                    </p> *@
                </div>
            </form>
        </section>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
